<!DOCTYPE html>
<html lang="en">
<head>
  <% include ../partials/head %>
</head>
<body>
  <header>
    <% include ../partials/header %>
  </header>
  <h2>Short URL: <strong><a href="/u/<%= url.shortUrl %>"><%= url.shortUrl %></a></strong></h2>
    <form method="POST" action="/urls/<%= url.shortUrl %>">
      <input type="text" name="longUrl" value="<%= url.longUrl %>" size="30">
      <input type="submit" name="updateButton" value="update">
    </form>
  </ul>
</body>
</html>